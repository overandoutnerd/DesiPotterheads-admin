{
  "name": "desipotterheads-admin",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "pnpm --filter client build",
    "dev": "pnpm --parallel --filter server --filter client dev"
  }
}
